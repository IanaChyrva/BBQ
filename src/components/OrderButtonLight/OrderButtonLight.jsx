import React from 'react';
import './OrderButtonLight.css';

class OrderButtonLight extends React.Component {
    render = () => (
        <button type="submit" className="OrderButtonLight">Заказать</button>
    )
};

export default OrderButtonLight;